_target_: uncertainty_planner.UncertaintyLatentPathSolver
checkpoint: ${policy}
batch_size: 1
num_samples: 64
flow_steps: 16
score_mode: rollout_goal
goal_weight: 1.0
consistency_weight: 0.0
smoothness_weight: 0.0
device: "cuda"
seed: ${seed}
history_size: 3
# Required: choose separately for each uncertainty target/checkpoint.
uncertainty_threshold: ???
uncertainty_source: auto
max_resample_rounds: 100
